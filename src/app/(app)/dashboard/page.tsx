/**
 * Tableau de bord — placeholder structurel. Le contenu réel (KPI par rôle,
 * `StatCard`) est un écran métier de Phase 2, voir
 * `docs/socle-frontend.md` §9.
 */
export default function DashboardPage() {
  return (
    <div className="space-y-2">
      <h1 className="text-xl font-semibold">Tableau de bord</h1>
      <p className="text-muted-foreground text-sm">
        Contenu à construire une fois les composants transverses (Phase 1)
        posés.
      </p>
    </div>
  );
}
