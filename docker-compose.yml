version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      # L'URL du backend, côté serveur uniquement
      - BACKEND_URL=http://host.docker.internal:3001
      - NODE_ENV=production
    restart: unless-stopped
